There are two functions in [main.py](main.py)

* `zellers()` implements the algorithm correctly, using integer (floor) division
* `zellers_error()`   implements the algorithm incorrectly, using floating point division
